# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
'on':
  push:
    branches:
      - develop
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Decrypt large secret
        run: ./.github/scripts/decrypt.sh
        env:
          PASS: ${{ secrets.PASS }}
      - name: Find and Replace
        env:
          MONGO_URL: ${{ secrets.MONGO_URL }}
        run: sed -i -e "s/__MONGO_URL__/$MONGO_URL/g" ./settings.json \
             sed -i -e "s/__GTM_ID__/123/g" ./settings.json
      - uses: meteorengineer/setup-meteor@v1
        with:
          meteor-release: '1.8.1'
      - name: Meteor Deploy
        run: METEOR_SESSION_FILE=$HOME/secrets/token.json DEPLOY_HOSTNAME=eu-west-1.galaxy-deploy.meteor.com meteor deploy afasdf12ea0-asdfadf.eu.meteorapp.com --owner knawat